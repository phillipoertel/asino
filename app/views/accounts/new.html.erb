
<h1>Neues Konto anlegen</h1>

<div class="alert alert-block">
asino greift Ihre Kontotransaktionen vom Saldomat RSS Feed ab. Werfen Sie bitte einen Blick in die Saldomat Einstellungen -> "RSS Server um" diesen zu aktivieren.
</div>

<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">  
      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :title, 'Name des Kontos' %>
    <%= f.text_field :title %>

    <%= f.label :feed, 'Lokale Saldomat RSS-Feed URL' %>
    <%= f.text_field :feed %>

  <p class="m20">
    <%= f.submit 'Speichern', :class => 'btn btn-primary' %> oder <%= link_to_function 'abbrechen', 'history.back(-1)' %>
  </p>
<% end %>
