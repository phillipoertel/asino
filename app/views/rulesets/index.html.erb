
<h1>Regeln</h1>

<div class="m20">
  <%= link_to 'Neue Regel', new_ruleset_path, :class => 'btn' %>
</div>

<% unless @rulesets.empty? %>
<table class="table">
  <thead>
    <tr>
      <th class="al">
        Name
      </th>
      <th class="al">Konto</th>
      <th class="al">Aktion</th>
      <th class="ac">
        Aktiv
      </th>
      <th style="width:40px;"></th>
    </tr>
  </thead>
  <tbody>
  <% @rulesets.each do |ruleset| %>
    <%= render :partial => 'rulesets/ruleset_row' , :locals => {:ruleset => ruleset }%>
  <% end %>
  </tbody>
</table>
<% else %>
<div class="m20">
  Bisher wurden keine Regeln angelegt.<br/>
  Mit Hilfe von Regeln können Sie neu eingehende Transaktionen automatisch Kategorien zuordnen.
</div>
  <%= link_to 'Neue Regel anlegen', new_ruleset_path, :class => 'button' %>
<% end %>
