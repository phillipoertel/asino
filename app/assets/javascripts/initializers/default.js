//
// this is run for all pages that don't have a page-specific initializer
//
Initializers.Default = function($) {
    
    console.log('Hello from the default initializer. I run when no one else is responsible.');
    
}